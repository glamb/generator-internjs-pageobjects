language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-internjs-pageobjects' ]; then cd .. && eval "mv $currentfolder generator-internjs-pageobjects" && cd generator-internjs-pageobjects; fi
